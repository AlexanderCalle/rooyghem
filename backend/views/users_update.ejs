<main class="container" id="backofficecontainer">
    <%- include('./particals/back_office.ejs') %>

    <div id="backofficecreation">
        <div>
            <h1>Update gebruiker</h1>

            <%- include('./particals/messages.ejs') %>

            <form action="/users/<%= userInfo.user_id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <label for="firstname">Voornaam </label>
                <input id="firstname" type="text" name="firstname" placeholder="Voornaam..." value="<%= userInfo.firstname %>">
                <br>
                <label for="lastname">Naam </label>
                <input id="lastname" type="text" name="lastname" placeholder="Naam..." value="<%= userInfo.lastname %>">
                <br>
                <label for="email">Email </label>
                <input id="email" type="email" name="email" placeholder="Email..." value="<%= userInfo.email %>">
                <br>
                <label for="username">Gebruikersnaam </label>
                <input  type="text" name="username" placeholder="Gebruikersnaam..." value="<%= userInfo.username %>">
                <br>
                <label for="phone">Telefoonnummer </label>
                <input id="phone" type="text" name="phone" placeholder="Telefoonnummer..." value="<%= userInfo.phone %>">
                <br>
                <% if(userInfo.is_banleader == 1) { %>
                    <input type="checkbox" name="is_banleader" id="is_banleader" value="1" checked>
                    <label for="is_banleader">Banleider</label>
                <% } else { %>
                    <input type="checkbox" name="is_banleader" id="is_banleader" value="1">
                    <label for="is_banleader">Banleider</label>
                <% } %>    
                <br>
                <% if(userInfo.is_admin == 1) { %>
                    <input id="is_admin" type="checkbox" name="is_admin" value="1" checked>
                    <label for="is_admin">Admin </label>
                <% } else { %>
                    <input id="is_admin" type="checkbox" name="is_admin" value="1">
                    <label for="is_admin">Admin </label>
                <% } %>   
                <br>
                <label for="bondsteam">Bondsteam</label>
                <select id="bondsteam" name="bondsteam">
                    <option> <%= userInfo.bondsteam %> </option>
                    <option>/</option>
                    <option>Bondsleider</option>
                    <option>Bondssecretaris</option>
                    <option>Bondspenningmeester</option>
                    <option>Volwassenen begeleiding</option>
                </select>
                <br>
                <label for="bannen">Ban </label>
                <select id="bannen" name="group_id">
                    <% groups.forEach(function(group) { %>
                        <% if(group.group_id == userInfo.group_id) { %>
                            <option value="<%= group.group_id %>"><%= group.name %></option>
                        <% } %>
                    <% }) %>
                    <% groups.forEach(function(group) { %>
                        <option value="<%= group.group_id %>"><%= group.name %></option>
                    <% }) %>
                </select>
                <br>
                <label for="picture_path">Foto van gebruiker </label>
                <input  type="file" name="image" placeholder="Leiders foto...">
                <br>
                <button type="submit">Update gebruiker</button>
            </form>
        </div>
    </div>

</main>